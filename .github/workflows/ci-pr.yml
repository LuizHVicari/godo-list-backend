name: CI - Pull Request

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  test:
    uses: ./.github/workflows/run-tests.yml

  lint:
    uses: ./.github/workflows/run-lint.yml

  sqlc-verify:
    uses: ./.github/workflows/run-sqlc-verify.yml
